#include <stdio.h>

/**
 * Main: Print with the function (printf)
 *
 * Return: 0 (Successful)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
