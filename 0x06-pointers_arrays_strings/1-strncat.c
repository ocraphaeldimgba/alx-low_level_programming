#include "main.h"
#include <string.h>

/**
 * _strncat - function to add char
 * @dest: first char
 * @src: second char
 * @n: third char
 * Return: String
 */
char *_strncat(char *dest, char *src, int n)
{
	strncat(dest, scr, n);
	return (dest);
}
